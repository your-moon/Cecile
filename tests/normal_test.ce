let i = 0;
type Foo {
    name: String
}
impl Foo {
    fn new(name: String) {
        self.name = name;
    }
}
let foo1 = Foo("init");
let arr: Vec<Foo> = [foo1];

while (i<10) {
    let foo_name = input();
    let foo = Foo(foo_name);
    arr.push(foo);
    i = i + 1;
}

println arr;
